[project]
name = "gitlab-merge-request-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4",
    "openai",
    "pocketflow>=0.0.2",
    "requests",
]
